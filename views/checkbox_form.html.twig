{% extends 'layout.html.twig' %}
{% set adminMenu = true %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
    <div class="well">
        {{ form_start(moderationForm) }}
        <div class="form-group">
            {{ form_label(moderationForm.published) }}
            {{ form_errors(moderationForm.published) }}
            {{ form_widget(moderationForm.published) }}
        </div>
        <div class="form-group">
            <div class="col-sm-offset-4 col-sm-3">
                <input type="submit"  class="btn btn-primary" value="moderer" />
            </div>
        </div>
        {{ form_end(moderationForm) }}
    </div>
{% endblock %}