<!-- jQuery -->
<script type="text/javascript" src={{ asset('lib/tinymce/tinymce.min.js') }}></script>.
<script src="{{ asset('/lib/bootstrap/js/bootstrap.js') }}"></script>
<script type="text/javascript">
    tinymce.init({
        forced_root_block : "",
        selector: 'textarea.tinyMce',
        language: 'fr_FR',
        statusbar: false,
        menu: {
            file: {title: 'File', items: 'newdocument'},
            edit: {title: 'Edit', items: 'undo redo | cut copy paste pastetext | selectall'},
            insert: {title: 'Insert', items: 'link media | template hr'},
            view: {title: 'View', items: 'visualaid'},
            format: {title: 'Format', items: 'bold italic underline strikethrough superscript subscript | formats | removeformat'},
            table: {title: 'Table', items: 'inserttable tableprops deletetable | cell row column'},
            tools: {title: 'Tools', items: 'spellchecker code'}},
        toolbar: false
    });

    tinyMCE.init({
        forced_root_block : "",
        language: 'fr_FR',
        selector: 'input#article_title',
        toolbar: false,
        statusbar: false,
        menu: {
            file: {title: 'File', items: 'newdocument'},
            edit: {title: 'Edit', items: 'undo redo | cut copy paste pastetext | selectall'},
            insert: {title: 'Insert', items: 'link media | template hr'},
            view: {title: 'View', items: 'visualaid'},
            format: {title: 'Format', items: 'bold italic underline strikethrough superscript subscript | formats | removeformat'},
            table: {title: 'Table', items: 'inserttable tableprops deletetable | cell row column'},
            tools: {title: 'Tools', items: 'spellchecker code'}}
    });

    //prevent empty article form
    $('.publish').click(function(e) {
        for (var i = 0;i < tinyMCE.editors.length; i++) {
            if (tinyMCE.activeEditor.getContent() === '') {
                $("#comment_content").attr('required','');
                alert ("remplissez le champ");
                e.preventDefault();
            }
            else if (tinyMCE.activeEditor.getContent() !== ''){
                $("#comment_content").removeAttr('required','');
            }
        }

    });

    //show comments on comment page
        var pixels = 0

    $('.childComment').each(function (index)
    {
        $(this).css('margin-left', pixels + 'px');
        pixels += 10;
        console.log(index);
        console.log(parent);
    });

    /*toggle comment form*/
    $("#commentForm").css('display','none');
    $( "#show" ).click(function() {
        $( "#commentForm" ).toggle("fade");
    });
</script>
